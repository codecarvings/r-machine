import "./resource-atlas.js"; // keep ResourceAtlas reachable so static phase succeeds

// Strategy export exists but does not implement CONFIG_ACCESSOR.
export const strategy = { notARealStrategy: true };
